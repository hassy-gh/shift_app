<% provide(:title, '従業員一覧') %>
<h1>従業員一覧</h1>
<h2><%= @group.name %></h2>

<div class="row">
  <div class="col-lg-6 mx-auto">
    <%= will_paginate %>
    <ul class="users">
      <%= render @users %>
    </ul>
    <%= will_paginate %>
  </div>
</div>
